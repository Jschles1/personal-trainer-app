<%= form_for @appointment do |f| %>
  <%= f.label :date, "Appointment Date & Time" %>
  <br></br>
  <%= f.datetime_select :date, order: [:month, :day, :year, :hour], ampm: true %>
  <br></br>
  <%= f.submit "Schedule Appointment" %>
<% end %>